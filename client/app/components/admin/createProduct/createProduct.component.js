import template from './createProduct.html';
import controller from './createProduct.controller';
import './createProduct.styl';

let createProductComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default createProductComponent;
